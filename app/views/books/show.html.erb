<div class="container">
 <div class="row content">
  <%= render 'users/list', user: @user, book: @book  %>
  <div class='col-xs-9'>
	<div class=success><%= h(flash[:success]) %></div>
	<div class=good><%= h(flash[:good]) %></div>

	<h2>Book detail</h2>


	<table class='table'>
	 <thead>
	    <tr>
	    	<th>title</th>
	    	<th>opinion</th>
	    </tr>
	 </thead>
	 <tbody>

	   <tr>

	   	   <td>
			     <%= link_to user_path(@books.user.id) do %>
                 <%= attachment_image_tag @books.user, :profile_image, :size => "60x60" %><br>
                 <%= @books.user.name %>

                 <% end %>
			</td>
			<td>
			<%= @books.title %>
			</td>
		   <td>
		    <%= @books.body %>
		   </td>
		   <td>
		   	<% if @books.user == current_user %>
			<%= link_to "Edit", edit_book_path(@one.id), class: "btn btn-success btn-default" %>
		    <% end %>
		   </td>
		   <td>
		   	<% if @books.user == current_user %>
	        <%= link_to "Destroy", book_path(@books.id),  method: :delete, "data-confirm" => "Are you sure?",class: "btn btn-danger btn-default" %>
	        <% end %>
		   </td>
	   </tr>

	　</tbody>
	 </table>
  </div>
 </div>
</div>

<footer>
  <div class='container'>
  	<div class='row'>
    	<div class='col-xs-3 col-xs-offset-9'>
    		<p>CopyRight Infratop.inc</p>
    	</div>
    </div>
  </div>
</footer>